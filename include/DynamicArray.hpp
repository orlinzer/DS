#ifndef DYNAMIC_ARRAY_H
#define DYNAMIC_ARRAY_H

#include <algorithm>
#include <iterator>
#include <iostream>
#include <string>
#include <cstring>

#include "Array.hpp"

template <typename T>
class DynamicArray : private Array {
  private:

  public:

    void resize (int size) {

    }

    void increase () {

    }
};

#endif // DYNAMIC_ARRAY_H
